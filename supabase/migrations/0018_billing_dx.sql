-- ════════════════════════════════════════════════════════════════
-- 메디코어 EMR · 청구에 주상병(진단) 추가
--
-- 실제 청구는 주상병코드(ICD-10/KCD) 기반 → billings 에 dx 컬럼 추가.
-- 값은 진단 마스터(diagnoses.code)와 같은 ICD-10/KCD 코드 텍스트.
-- 기존 행은 빈 문자열 기본값(하위호환). 청구 요약 뷰는 영향 없음.
--
-- 적용 순서:  0001 → … → 0017 → 0018 → seed
-- ════════════════════════════════════════════════════════════════

alter table billings add column if not exists dx text not null default '';  -- 주상병 ICD-10/KCD 코드
